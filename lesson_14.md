1) Написать скрипт, который 
    - собирает load average с удаленных хостов (значение за 5 минут)
    - сравнивает с числом CPU и если `load > CPU * 2`, то выводит сообщение об этом в файл `remote_load.log`
2) Написать скрипт, который делает проверку доступности сайта (`curl`). Скрипт должен делать 5 попыток с интервалом 3 секунды и выводить предупреждение, если 3 поптыки подряд провалились.
3) Написать сркипт, который создает архив указанной в аргументе директории и отправляет его на удаленный сервер в директорию `/opt/backups`
