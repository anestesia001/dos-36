1) Написать скрипт, который ищет в каталоге все файлы старше N дней и выводит, какие из них можно безопасно удалить (N задается позиционным аргументом).
2) Написать скрипт для мониторинга доступности хоста (можно использовать `ping`) с записью результата в лог с датой и временем (формат произвольный).
3) Написать скрипт, который читает список серверов из файла и проверяет на них:
    - доступность SSH;
    - свободное место;
    - load average;
4) Написать скрипт, который выводит список пользователей с shell, отличным от `/usr/sbin/nologin` или `/bin/false`
